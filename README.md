Contains a python script that is to be executed as a lambda function.

Contains a CFN template that uses the ARN of the lambda function to create the CustomResource - in this case, the security group.